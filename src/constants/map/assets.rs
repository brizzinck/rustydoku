use crate::constants::square::assets::{
    SQUARE_IMAGE_FIRST_DEACTIVATED_PATH, SQUARE_IMAGE_SECOND_DEACTIVATED_PATH,
};

/// Path to the first style of deactivated tile image (mapped from square assets).
pub const TILE_IMAGE_FIRST_DEACTIVATED_PATH: &str = SQUARE_IMAGE_FIRST_DEACTIVATED_PATH;

/// Path to the second style of deactivated tile image (mapped from square assets).
pub const TILE_IMAGE_SECOND_DEACTIVATED_PATH: &str = SQUARE_IMAGE_SECOND_DEACTIVATED_PATH;

use crate::constants::ui::assets::FONT_PATH_DEFAULT;

/// Path to the background image for the game over panel.
pub const GAME_OVER_BACKGROUND_PATH: &str = "embedded://ui/game_over_panel_back.png";

/// Path to the font used for the header title in the game over panel.
/// Now default [`FONT_PATH_DEFAULT`], easy to change in the future.
pub const GAME_OVER_HEADER_FONT_PATH: &str = FONT_PATH_DEFAULT;

/// Path to the font used for the score text in the game over panel.
/// Now default [`FONT_PATH_DEFAULT`], easy to change in the future.
pub const GAME_OVER_SCORE_FONT_PATH: &str = FONT_PATH_DEFAULT;

/// Path to the font used for the max score text in the game over panel.
/// Now default [`FONT_PATH_DEFAULT`], easy to change in the future.
pub const GAME_OVER_MAX_SCORE_FONT_PATH: &str = FONT_PATH_DEFAULT;
